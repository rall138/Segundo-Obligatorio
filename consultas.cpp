/*#include "consultas.h"

int conCantidadConsultasMayoresAFecha(Consultas Cons, Fecha fecha)
{
    int cantCons=0;
    while (Cons!=NULL)
    {
        if(fecha==Cons->consulta.fecha)
            cantCons++;
        Cons=Cons->sig;
    }
    return cantCons;
}
*/
